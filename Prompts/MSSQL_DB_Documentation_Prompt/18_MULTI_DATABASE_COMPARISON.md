# Multi-DB Comparison Prompt
When multiple DBs are configured, create independent outputs plus comparison reports for schemas/tables/columns/types/PK/FK/index/constraints/definitions/hashes/object counts/row counts/safe lookup values. Never merge data or assume same numeric IDs share meaning.
