# Connection Prompt
Support Windows Integrated Auth and SQL Auth via env; single MSSQL_DATABASE or comma-separated MSSQL_DATABASES; CLI --database override; ODBC detection; sanitized capability report; isolated per-DB outputs; never enumerate all DBs automatically.
