# Programmable Objects Prompt
Inventory views, SPs, scalar/TVFs, triggers, synonyms, sequences, computed expressions and SQL Agent metadata if visible. Capture definitions, parameters, hashes, dependencies, likely reads/writes, nested calls, temp tables, dynamic SQL, transactions/error handling. Never execute them.
