# Metadata Prompt
Inventory whole configured DB: schemas, tables, columns, PK/FK/unique/check/default constraints, indexes, computed columns, extended properties, temporal/system metadata, storage metadata. Export CSV/JSON/Markdown and flag missing PKs, untrusted constraints and FK-like undeclared columns.
