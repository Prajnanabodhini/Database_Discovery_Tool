# Configuration boundary

Runtime configuration is environment-backed. Do not place credentials in this directory. Use the ignored root `.env` file or process environment and keep `.env.example` as the non-secret schema.
