# SQL boundary

Executable discovery SQL remains in the typed, registered Python query catalogues under `src/mssql_database_documenter/`. The application has no free-form SQL endpoint. This directory is reserved for reviewed, non-secret SQL assets if the registry is separated in a future release.
